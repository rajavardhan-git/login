1. Execute the SQL query to create the users table inside your MySQL database.
2. "config.php" will create a PHP script in order to connect to the MySQL database server.
3. "register.php" will create a web form that allows user to register themselves.
4. "welcome.php" file, where user is redirected after successful login.
5. The script inside "logout.php" file destroys the session and redirect the user back to the login page.
6. 